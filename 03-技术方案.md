# 技术方案

## 技术栈

复用 AI Detector 项目已验证的框架，降低开发风险。

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 框架 | Next.js 16 | App Router，静态导出 |
| 语言 | TypeScript | 类型安全 |
| 样式 | Tailwind CSS | 快速布局 |
| 部署 | Cloudflare Pages | 免费套餐，全球 CDN |
| 域名 | Cloudflare Registrar | counteruavhub.com |
| 分析 | Google Analytics 4 | 流量追踪 |
| 搜索收录 | Google Search Console | SEO 监控 |

## 项目结构

```
counteruavhub/
├── web/                         # Next.js 前端，Cloudflare Pages 静态导出
│   ├── app/
│   │   ├── page.tsx             # 首页
│   │   ├── tools/               # 数据库和 RF 计算器
│   │   ├── bands/[band]/        # 频段 SEO 聚合页
│   │   ├── drones/[slug]/       # 无人机型号详情页
│   │   ├── blog/[slug]/         # Markdown 文章详情页
│   │   ├── news/                # 新闻聚合页
│   │   └── about/
│   ├── components/              # 公共组件
│   ├── data/
│   │   ├── drones.json          # 机器可写无人机频率数据
│   │   ├── drones.ts            # 类型定义和导出入口
│   │   ├── news.json            # 机器可写新闻数据
│   │   └── news.ts              # 类型定义和导出入口
│   ├── scripts/                 # 新闻/型号更新脚本和测试
│   ├── lib/                     # 博客、analytics、频段页辅助逻辑
│   └── content/blog/            # Markdown 文章
└── package.json
```

## 数据方案

当前频率数据采用静态 JSON + TypeScript 导出入口，无需运行时数据库。

数据结构：
```typescript
interface DroneRecord {
  id: string
  name: string
  brand: string
  category: 'consumer' | 'industrial' | 'fpv' | 'military'
  controlFreq: string      // e.g. "2.4GHz / 5.8GHz"
  videoProtocol: string    // e.g. "DJI O4"
  videoFreq: string        // e.g. "5.8GHz"
  gpsFreq: string          // e.g. "1.575GHz (L1)"
  maxTxPower: string
  counterFreq: string      // 建议反制频率
  source: string           // 数据来源
  sourceTier: 'official' | 'fcc' | 'third-party'
  sourceUrl?: string
  controlFreqMHz: number[]
  gcsTxPowerDbm: number
  controlChannelBW_MHz: number
}
```

## 内容方案

博客文章使用 Markdown 文件存储在 `content/blog/` 目录，通过 Next.js 静态生成页面。

## 部署方案

- `web/next.config.ts` 使用 `output: 'export'` + `trailingSlash: true`
- Git push 到 GitHub 主分支 → Cloudflare Pages 自动部署
- 构建命令：`npm run build`
- 输出目录：`out`
- 新闻和无人机数据由 GitHub Actions 定时更新 JSON，提交 main 后触发 Cloudflare Pages

## SEO 技术配置

- `next/metadata` 配置每页 title / description
- `app/sitemap.ts` 自动生成 sitemap.xml
- JSON-LD schema markup（工具页用 WebApplication，文章页用 Article）
- OpenGraph 图片（静态生成）
